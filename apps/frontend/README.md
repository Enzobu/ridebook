# Frontend Ridebook

SPA React/Vite de Ridebook.

## Commandes

```bash
pnpm --filter frontend dev
pnpm --filter frontend build
pnpm --filter frontend lint
pnpm --filter frontend test
```

Les features frontend seront organisées dans `src/features/`.
