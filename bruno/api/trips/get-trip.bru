meta {
  name: Get trip
  type: http
  seq: 3
}

get {
  url: {{apiUrl}}/api/v1/trips/{{tripId}}
  body: none
}

docs {
  Consulte une balade publique non supprimée.
}

tests {
  expect([200, 404]).to.include(res.status);
}
