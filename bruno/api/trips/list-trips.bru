meta {
  name: List trips
  type: http
  seq: 1
}

get {
  url: {{apiUrl}}/api/v1/trips?page=1&limit=20
  body: none
}

docs {
  Liste publique des balades non supprimées, avec pagination, recherche, tri et filtre par statut.
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.items).to.be.an("array");
}
