meta {
  name: Delete trip
  type: http
  seq: 5
}

delete {
  url: {{apiUrl}}/api/v1/trips/{{tripId}}
  body: none
}

docs {
  Supprime logiquement une balade via `deletedAt`. Réservé au propriétaire ou à un administrateur.
}

tests {
  expect([204, 401, 403, 404]).to.include(res.status);
}
