meta {
  name: Me
  type: http
  seq: 4
}

get {
  url: {{apiUrl}}/api/v1/auth/me
  body: none
}

docs {
  Retourne l'utilisateur connecté depuis le cookie access token.
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.user.role).to.be.oneOf(["ADMIN", "USER"]);
}
