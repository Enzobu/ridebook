meta {
  name: Refresh
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/api/v1/auth/refresh
  body: none
}

docs {
  Renouvelle le couple access/refresh token avec rotation du refresh token.
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.user.id).to.be.a("string");
}
